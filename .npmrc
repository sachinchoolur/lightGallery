shamefully-hoist=true
strict-peer-dependencies=false
# Resolve semver ranges against workspace packages (ng-packagr copies the
# Angular package's manifest verbatim, so it must carry the real published
# range for @lightgallery/headless, not the workspace: protocol).
link-workspace-packages=true
